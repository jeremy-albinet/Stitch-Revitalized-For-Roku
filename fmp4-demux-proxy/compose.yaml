services:
  fmp4-demux-proxy:
    build: .
    ports:
      - "${FMPDEMUX_PORT:-8080}:8080"
    environment:
      - PORT=8080
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - PROXY_PUBLIC_URL=${PROXY_PUBLIC_URL:-}
    restart: unless-stopped
